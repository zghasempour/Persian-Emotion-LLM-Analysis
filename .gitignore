# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Environment / secrets — never commit API keys
.env
*.key

# Experiment outputs (regenerated by compare.py; commit curated copies deliberately)
results.csv
results_*.csv

# Claude Code local (personal) settings — keep shared config, ignore local overrides
.claude/settings.local.json

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db
